// path
const PATH_ROOT = {
  SRC : "./src/",
  DIST: "./dist/",
};
const PAGES = {
  SRC: `${PATH_ROOT.SRC}pages/`,
  DIST: `${PATH_ROOT.DIST}`,
};
const DATA = {
  SRC: `${PATH_ROOT.SRC}data/`,
  DIST: `${PATH_ROOT.DIST}`,
};



// exports
exports.PATH_ROOT = PATH_ROOT;
exports.PAGES = PAGES;
exports.DATA = DATA;
